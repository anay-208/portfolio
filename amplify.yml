version: 1
<<<<<<< HEAD
backend:
  phases:
    build:
      commands:
        - nvm use 20
        - npm ci
        - npx amplify pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
=======
>>>>>>> ac4adf8ddff736af25cfe9e6a0da6d0c343accb5
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
